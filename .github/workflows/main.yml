on: push
name: 🚀 Deploy website on push
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v3

    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@v4.3.4
      with:
        server: 161.35.125.23
        username: root
        password: aTlas00z
        exclude: |
          **/.git*
          **/.git*/**
          **/node_modules/**
          /usr/share/nginx/html/index.txt
